function probability = gaussian_distribution(X, mean_value, variance)
  % exponents = zeros(size(X, 1), 1);
  % for i = 1 : n
    % aux = X(i, :) - mean_value;
    % exponents(i) = -0.5 * aux' * aux / variance;
  % endfor
 
  % denominator = sqrt((2 * pi) * variance));
  % probability = exp(exponent) / denominator;
endfunction
